# DESIGN.md — B&F Global MES 디자인 기준

## 방향 (한 줄)
"공장의 스테인리스 작업대 위에 놓인 잘 만든 도구" — 따뜻한 뉴트럴 + 단 하나의 오렌지, 밀도 있고 조용한 Operate UI.

## 컬러 (사용자 고정 — 변경 금지)
- 액센트(유일): `--p:#D94F1E` / hover `--pd:#B83E15` / soft `--pb:#FFF5F0`
- 액센트는 **주요 액션·현재 선택·상태 표시에만**. 장식 금지.
- 뉴트럴은 오렌지에서 파생된 **웜 그레이 한 계열만** (쿨그레이 혼용 금지):
  - 배경 `#FAF7F5` · 카드 `#FFFFFF` · 사이드바(2차 뉴트럴) `#F6F1ED`
  - 보더 `#E9E2DC` · 본문 `#221A14` · 보조텍스트 `#75695F`(4.5:1↑) · 플레이스홀더 `#8A7D72`
- 의미색: 성공 `#15803D` · 경고 `#B45309` · 오류 `#DC2626` · 정보 `#2563EB` (상태에만)

## 타이포그래피
- 단일 패밀리: **Pretendard Variable** (jsdelivr CDN, dynamic-subset) → 폴백 'Noto Sans KR', sans-serif
  - Inter/Roboto/시스템 기본 금지(브랜드 없음). 세리프 금지(Operate).
- 고정 rem 스케일, 비율 1.125: 11 / 12 / **13(기준)** / 15 / 17 / 19 / 22px
- 페이지 제목 19px·800 단색(그라데이션 텍스트 금지), 자간 -0.02em
- 숫자는 어디서나 `font-variant-numeric: tabular-nums` (ERP 정렬 필수)

## 형태
- 라운드 규칙 문서화: 카드 12px · 버튼/입력 9px · 칩/뱃지 pill. 이 규칙 외 값 금지.
- 그림자: 오프셋+블러 필수, 배경 휴로 틴트(`rgba(60,35,20,…)`), 순검정 금지. 카드 기본은 보더 우선, 그림자는 아주 얕게.
- 카드 안 카드 금지 — 내부 구획은 보더탑/여백으로.
- 컬러 border-left 두껍게(>1px) 금지 (네비 활성 인디케이터 제외).

## 모션 (Operate: 상태 전달만, 장식 금지)
- 150–250ms, `cubic-bezier(.2,0,0,1)` ease-out. **바운스/오버슈트 금지.**
- 페이지 전환: 140ms 페이드만. 카드 순차 등장·로드 연출 금지.
- 허용: 버튼 press scale(.98), 포커스 링, 모달 180ms 팝(오버슈트 없음), 상태 배지 변화
- 금지: 흐르는 그라데이션, 무한 펄스 장식, hover 시 아이콘 확대 같은 비상태 모션
- `prefers-reduced-motion` 존중(전역)

## 절대 금지 (AI 티 5종 + Operate 규칙)
1. Inter/시스템 기본 글꼴 그대로
2. 보라·파랑 그라데이션 배경 (오렌지 브랜드 외 그라데이션 전반 자제)
3. 카드 안의 카드
4. 유색 배경 위 회색 글자 (틴트된 보조색 사용)
5. 튕기는 바운스 이징
6. 그라데이션 텍스트 / 장식용 blur·glass / 데이터 대신 스파크라인 장식
